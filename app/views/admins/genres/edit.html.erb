<h2>ジャンル編集</h2>
<div>
<%= form_with model: @genre, url: admins_genre_path, local:true do |f| %>
    <div class="form-group">
      <label>ジャンル名</label>
      <%= f.text_field :name, class: "form-control genre-name", placeholder:"ジャンル名" %>　<%= f.submit '変更を保存' %>
    </div>
<% end %>
</div>