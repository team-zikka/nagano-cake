  <h2>商品一覧</h2>
    <% @items.each do |item| %>
      <%= link_to item_path(item.id) %>
        <%= attachment_image_tag(item, :image, size: "120x120") %>

         <p><%= item.name %></p>
         <P>税込価格を数字だけ表示</P>

    <% end %>
